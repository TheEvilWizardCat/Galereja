<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Galereja.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="20"
            Spacing="10">
            <Entry Placeholder="Nazwa" Text="{Binding NewName}"/>
            <Entry Placeholder="Numer" Text="{Binding NewPhoneNumber}"/>
            <Entry Placeholder="Link obrazu" Text="{Binding NewImageUrl}"/>

            <Button Text="Dodaj kontakt" Command="{Binding AddContactCommand}"/>

            <CollectionView ItemsSource="{Binding Contacts}">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="4" HorizontalItemSpacing="10" VerticalItemSpacing="10"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="LightGray" CornerRadius="10" Padding="10" Margin="5">
                            <VerticalStackLayout>
                                <Image Source="{Binding ImageUrl}" WidthRequest="100" HeightRequest="100" Aspect="AspectFill"/>
                                <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Center"/>
                                <Label Text="{Binding PhoneNumber}" FontSize="14" HorizontalTextAlignment="Center"/>
                            </VerticalStackLayout>

                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
